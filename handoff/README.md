# Handoff

This folder reserves future Chatty ecosystem handoff lanes for Chatty Quest.

Important:

- these lanes are not active dependencies in `v0.1`
- they exist to make future seams obvious
- real handoff behavior should remain explicit, mediated, and metadata-rich when it is added later
