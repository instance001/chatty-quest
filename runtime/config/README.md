# Runtime Config

Reserved for local application settings and future user-specific runtime configuration.

`v0.1` may leave this mostly placeholder unless lightweight app settings become useful early.
