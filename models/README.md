# Models

This folder is reserved for future local model integration.

Long-term possible roles:

- narrator adapters
- capsule helpers
- local assistant or summarization helpers

`v0.1` uses `MockNarrator` only and does not require live models.
