# Datasets

This folder is reserved for future worldbuilding, template generation, and other dataset-oriented workflows.

It is present to make the long-term architecture obvious, not because `v0.1` depends on dataset tooling.
